user     = ${EMAIL_DB_USER}
password = ${EMAIL_DB_PASSWORD}
hosts    = ${EMAIL_DB_HOST}
dbname   = ${EMAIL_DB_NAME}
query    = SELECT goto FROM alias,alias_domain WHERE alias_domain.alias_domain = '%d' and alias.address = CONCAT('@', alias_domain.target_domain) AND alias.active = TRUE AND alias_domain.active='1'
